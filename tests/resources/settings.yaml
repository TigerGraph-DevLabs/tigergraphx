vector_db:
  type: "LanceDB"
  table_name: "test_table_name"
  uri: "test_uri"
  api_key: "test_api_key"
  region: "test_region"
  host_override: "test_host_override"


llm:
  type: "OpenAI"
  # NOTE: The api_key must be provided via the environment variable OPENAI_API_KEY
  # base_url: ""
  # organization: ""
  # max_retries: 5
  # request_timeout: 150.0

embedding:
  type: "OpenAI"
  model: "text-embedding-3-small"
  # max_tokens: 8191
  # max_retries: 10
  # request_timeout: 180.0
  # encoding_name: "cl100k_base"

chat:
  type: "OpenAI"
  model: "gpt-4o-mini"
  # max_retries: 10
